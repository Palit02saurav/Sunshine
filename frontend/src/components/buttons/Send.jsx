import React from "react";

const Send_Button = () => {
  return (
    <button
      className="send_btn"
      type="submit"
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      SEND
    </button>
  );
};

export default Send_Button;